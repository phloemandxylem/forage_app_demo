source 'https://rubygems.org'
 gem 'rails', '4.2.5'

 group :production do
   gem 'pg'
   gem 'rails_12factor'
 end

 group :development do
   gem 'sqlite3'
 end

 # Use SCSS for stylesheets
 gem 'sass-rails', '~> 5.0'
 # Use Uglifier as compressor for JavaScript assets
 gem 'uglifier', '>= 1.3.0'
 # Use CoffeeScript for .coffee assets and views
 gem 'coffee-rails', '~> 4.1.0'
 # Use jquery as the JavaScript library
 gem 'jquery-rails'
 # Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks
 gem 'turbolinks'
 # Use Devise for authentication
 gem 'devise'
 gem 'bootstrap-sass'
 #Bootstrap themese. See http://bootswatch.com/ for details
 gem 'bootswatch-rails'
 #Figaro assd a simple, Heroku-friendly Rails app configuration using ENV and a single YAML file
 gem 'figaro', '1.0'
 #faker is used to generate random data to seed the database
 gem 'faker'
 #Used to automate cron jobs
 #gem 'whenever'
 #Add Markdown
 gem 'redcarpet'
 #Add a greatly enhanced rails console
 gem 'pry-rails', :group => :development
 #Add authorization
 gem 'pundit'
 #Add pagination
 gem 'will_paginate-bootstrap'
 #Add Stripe for credit card processing
 gem 'stripe'
